services:
  httpd:
    image: dhi.io/httpd:2.4.68-debian13
    container_name: my-httpd
    ports:
      - "8080:8080"
      - "443:443"
    environment:
      - HTTPD_PREFIX=/usr/local/apache2
    volumes:
      - /home/app/apache2/htdocs:/usr/local/apache2/htdocs:ro
      
